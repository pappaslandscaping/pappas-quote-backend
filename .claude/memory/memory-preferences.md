# User Preferences
- App should look like Jobber, operate like home.works
- Everything connected as one system, not separate pages
- Brand colors: forest green #2e403d, lime #c9dd80
- Prefers Claude make decisions rather than ask too many questions
- NEVER delete or overwrite working code — existing features must be preserved. Refactoring that removes endpoints is not acceptable. Only add/edit, don't remove unless explicitly asked.
- Does not know SQL — build admin UI tools for any database operations
- Does not want to resend emails to customers for technical issues — fix the root cause instead
